{
  "name": "genesis-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unit tests for genesis embeddings endpoint",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose",
    "pretest": "node -e \"try{require.resolve('next/server')}catch{console.error('\\nRun npm install from the repo root first (tests import src/ and open-sse/).\\n');process.exit(1)}\""
  },
  "devDependencies": {
    "open": "^11.0.0",
    "uuid": "^13.0.0",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
